# DBS-Group-Project
To run server
- Make sure Vite is installed in "frontend". "npm vite -v" will check the version and "npm install vite" will install it
- Make a build of the app. Run "npm run build"
- Run "npm run preview" to start the localhost.